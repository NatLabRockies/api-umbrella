includes:
  - ./rc-log-common.yaml

service:
  log_file: "${API_UMBRELLA_LOG_DIR}/fluent-bit.log"

pipeline:
  outputs:
    - name: file
      match: "*"
      file: current
      path: ${API_UMBRELLA_LOG_DIR}
