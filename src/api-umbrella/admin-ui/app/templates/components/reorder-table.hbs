{{#if this.isReorderable}}
  <button type="button" id="sub_settings_reorder" class="btn btn-light btn-sm" {{on "click" this.toggleReordering}}>
    <FaIcon @icon="grip-vertical" />
    {{#if this.isReordering}}
      Done
    {{else}}
      Reorder
    {{/if}}
  </button>
{{/if}}
